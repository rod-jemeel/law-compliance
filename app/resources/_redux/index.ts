"use client";

import resourcesReducer from "./resources-slice";
import {
  saveResourcesPreferences,
  loadResourcesPreferences,
} from "./resources-storage";

export { resourcesReducer, saveResourcesPreferences, loadResourcesPreferences };
